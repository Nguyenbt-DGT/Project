import { PlaceholderScreen } from '@/components/placeholder-screen';

export function HealthCheckScreen() {
  return (
    <PlaceholderScreen
      title="HEALTH_CHECK"
      description="Vehicle metrics, maintenance intervals, and service status (KB §2)."
    />
  );
}
