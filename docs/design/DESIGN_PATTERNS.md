# Shared Design Patterns — Moto Companion App

> **Owner**: `designer` agent. **Referenced by**: `designer.md` agent instructions. This is **not**
> a full design system — just enough of a running list to keep the same concept (e.g. how
> severity/warning levels are shown) from being re-invented differently across HEALTH_CHECK,
> TOURING_PLAN, and MAP_TRACKING. Final branding/component-library decisions stay out of scope
> (see `designer.md`).

| Pattern | Where used | Description | Established in |
|---|---|---|---|
| _(none recorded yet — e.g. the four-state wear severity coloring in `health-check/logic/status.ts` is a candidate first entry once designer confirms the visual mapping)_ | | | |
